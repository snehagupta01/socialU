<link rel="stylesheet" href="/css/user_profile.css">
<% if(false) {%>
//comment
so this link tag isfloating in body no, to place it in head
//tell express to extract style and script and place them in head & in the end of body no
<%}%>

<h1>SocialU <%=title%> </h1>

<section id="feed-posts">
        <h4>Posts</h4>
        <% if(locals.user) {%>
        <form action="/posts/create" id="new-post-form" method="POST">
                <textarea name="content" rows="3" cols="30" placeholder="type here..." required></textarea>
                <input type="submit" value="POST"> 
        </form>
        <%}%>
        <div id="posts-list-container">
                <ul>
                        <% for (post of posts) {%>
                                <%- include('_post') -%>
                        <% } %>
                </ul>
        </div>
</section>

<section id="user-friends">
        <h4>Friends</h4>
        <% for(u of all_users) { %>
                <p> <a href="/users/profile/<%= u.id %>"> <%= u.name %> </a></p>
        <% } %>
</section>